/**
 * Video Call Feature Module
 * 
 * Handles in-app video calling with matches.
 */

// Video call components
export { VideoCallProvider, useVideoCallContext } from "@/components/VideoCallProvider";
export { VideoCallModal } from "@/components/VideoCallModal";
export { VideoCallButton } from "@/components/VideoCallButton";

// Video call hooks
export { useVideoCall } from "@/hooks/use-video-call";
